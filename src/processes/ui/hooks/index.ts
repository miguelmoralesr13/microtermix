/**
 * useListeningProcesses - Hook for scanning and managing listening processes.
 */
export {
  useListeningProcesses,
  useTerminateProcess,
  filterByProtocol,
  filterManaged,
  filterByPort,
  groupByState,
  isManagedByMicrotermix,
  extractPort,
  processKeys,
} from './useListeningProcesses';
